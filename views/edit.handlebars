<h1 class="mb-1 restaurant-show-title">修改{{item.name}}</h1>
<div class="container">
  <div class="row">
    <div class="col-12 col-md-10 mx-auto" id="restaurants-wrapper">
      <form action="/restaurants/{{item._id}}?_method=PUT" method="POST" class="needs-validation" novalidate>
        <div class="form-row">
          <div class="form-group col-md-4">
            <label for="name" class="text-secondary">
              <i class="fas fa-utensils pr-1"></i>
              名稱*
            </label>
            <input type="text" class="form-control" name="name" id="name" placeholder="餐廳名稱" value="{{item.name}}" required>
            <div class="invalid-feedback">請輸入餐廳名稱。</div>
          </div>
          <div class="form-group col-md-3">
            <label for="name_en" class="text-secondary">
              <i class="fas fa-pen-nib pr-1"></i>
              英文名稱
            </label>
            <input type="text" class="form-control" name="name_en" id="name_en" aria-describedby="name_en_Help" placeholder="English name" value="{{item.name_en}}">
            <small id="name_en_Help" class="form-text text-muted">如果有英文名稱可以輸入</small>
          </div>
          <div class="form-group col-md-3">
            <label for="category" class="text-secondary">
              <i class="fas fa-filter pr-1"></i>
              類別*
            </label>
            <input list="category-list" type="text" class="form-control" name="category" id="category" aria-describedby="categoryHelp" placeholder="選擇餐廳類型" value="{{item.category}}" required>
            <datalist id="category-list">
              {{#each category}}
                <option>{{this}}</option>
              {{/each}}
            </datalist>
            <div class="invalid-feedback">請輸入餐廳分類。</div>
            <small id="categoryHelp" class="form-text text-muted">找不到相關分類可以自行輸入</small>
          </div>
          <div class="form-group col-md-2">
            <label for="rating" class="text-secondary">
              <i class="fas fa-star pr-1"></i>
              評分*
            </label>
            <input type="number" min="0" max="5" step="0.1" class="form-control" name="rating" id="rating" placeholder="4.5" value="{{item.rating}}" required>
            <div class="invalid-feedback">請輸入 0 ~ 5 間的數字範圍。</div>
          </div>
        </div>
        <div class="form-group">
          <label for="image" class="text-secondary">
            <i class="fas fa-camera pr-1"></i>
            圖片*
          </label>
          <input type="url" class="form-control" name="image" id="image" placeholder="http://" value="{{item.image}}" required>
          <div class="invalid-feedback">請輸入正確的網址連結。</div>
        </div>
        <div class="form-row">
          <div class="form-group col-md-8">
            <label for="location" class="text-secondary">
              <i class="fas fa-map-marker-alt pr-1"></i>
              地址
            </label>
            <div class="d-flex">
              <input type="text" class="form-control" name="location" id="location" aria-describedby="locationHelp" placeholder="餐廳地址" value="{{item.location}}">
              <a class="btn btn-major" href="#" id="map-button" role="button" disabled><i class="fas fa-search"></i></a>
            </div>
            <small id="locationHelp" class="form-text text-muted">輸入地址後點擊右方按鈕可以直接連結至 google map ，方便尋找確切店家位置</small>
          </div>
          <div class="form-group col-md-4">
            <label for="phone" class="text-secondary">
              <i class="fas fa-map-marker-alt pr-1"></i>
              餐廳電話
            </label>
            <input type="text" maxlength="12" pattern="[ 0-9]+" class="form-control" name="phone" id="phone" placeholder="02 2363 8009" value="{{item.phone}}">
            <div class="invalid-feedback">請輸入半形數字。</div>
          </div>
        </div>
        <div class="form-group">
          <label for="google_map" class="text-secondary">
            <i class="fas fa-location-arrow pr-1"></i>
            google map
          </label>
          <input type="url" class="form-control" name="google_map" id="google_map" placeholder="http://" value="{{item.google_map}}">
          <div class="invalid-feedback">請輸入正確的網址連結。</div>
        </div>
        <div class="form-group">
          <label for="description" class="text-secondary">
            <i class="fas fa-info-circle pr-1"></i>
            簡介
          </label>
          <textarea type="area-text" class="form-control" name="description" id="description" placeholder="一段簡介或描述" rows="3">{{item.description}}</textarea>
        </div>
      <div class="d-flex justify-content-center mb-3">
        <button type="submit" class="btn btn-major mx-2 ml-auto">Submit</button>
        <button type="reset" class="btn btn-secondary mx-2">Reset</button>
        <button type="button" class="btn btn-danger ml-auto"  data-toggle="modal" data-target="#delete-modal" data-id="{{ item._id }}" data-name="{{item.name}}">Delete</button>
      </div>
      </form>
    </div>
  </div>
</div>
<script src="/javascripts/bootstrapForm.js"></script>
<script src="/javascripts/forNewForm.js"></script>